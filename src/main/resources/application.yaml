spring:
  application:
    name: find_me_a_doc_backend
  datasource:
    url: jdbc:postgresql://localhost:5432/findmeadoc
    username: findmeadoc_user
    password: iit@9009
    driver-class-name: org.postgresql.Driver
    jpa:
      hibernate:
        ddl-auto: update
      show-sql: true
      properties:
        hibernate:
          dialect: org.hibernate.dialect.PostgreSQLDialect
